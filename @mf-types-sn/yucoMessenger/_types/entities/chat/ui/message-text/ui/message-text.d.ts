export declare const MessageText: ({ text, maxWidth, suffixWidth, font }: {
    text: string;
    maxWidth: number;
    suffixWidth: number;
    font: string;
}) => import("react/jsx-runtime").JSX.Element;
