export declare const useMessageText: ({ text, maxWidth, suffixWidth, font }: {
    text: string;
    maxWidth: number;
    suffixWidth: number;
    font: string;
}) => {
    messageLines: {
        line: string;
        key: string;
    }[];
};
